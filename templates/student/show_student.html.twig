{% extends 'base.html.twig' %}

{% block title %}{{ 'Student' }}{% endblock %}

{% block logout %}{% endblock %}

{% block welcome %}{% endblock %}

{% block page_header %}{% endblock %}

{% block content %}

    <!-- Single button -->
    <div class="card">
        <div class="card-header bg-white header-elements-inline">
            <h4 class="card-title"><i class="icon-list mr-2"></i> {{ 'Students List' }}</h4>
            <div class="header-elements">
                <a href="{{ path('student_new') }}" class="btn btn-labeled btn-labeled-right bg-blue"><i class="icon-add"></i> {{ 'new'|upper }}</a>
            </div>
        </div>

        <!-- <div class="card-body">
            Header with basic button. Available in all sizes, colors and options
        </div> -->
    </div>
    <!-- /single button -->

    <!-- Basic responsive configuration -->
    <div class="card">

        <table class="table datatable-responsive table-bordered table-striped table-hover">
            <thead class="bg-blue">
            <tr>
                <th scope="col">{{ 'Register'|upper }}</th>
                <th scope="col">{{ 'Last Name'|upper }}</th>
                <th scope="col">{{ 'First Name'|upper }}</th>
                <th scope="col">{{ 'File'|upper }}</th>
                <th scope="col">{{ 'Email'|upper }}</th>
                <th scope="col">{{ 'Phone'|upper }}</th>
                <th scope="col">{{ 'Group'|upper }}</th>
                <th scope="col"><i class="fa fa-calendar" aria-hidden="true"></i> {{ 'Creation Date'|upper}}</th>
                <th class="text-center"></i> {{ 'actions'|upper }}</th>
            </tr>
            </thead>
            <tbody>
            {% for student in students %}
                <tr>
                    <td>{{ student.register }}</td>
                    <td>{{ student.lastName }}</td>
                    <td>{{ student.firstName }}</td>
                    <td>{{ student.file }}</td>
                    <td>{{ student.email }}</td>
                    <td>{{ student.phone }}</td>
                    <td>{{ student.groupe }}</td>
                    <td>{{ student.createdAt|date("d/m/Y") }}</td>
                    <td class="text-center">
                        <div class="list-icons">
                            <a href="{{ path('student_edit', {id:student.id}) }}" data-popup="tooltip" title="{{ 'update' }}" data-container="body" class="list-icons-item form-icon text-primary-600">
                                <i class="icon-pencil7"></i>
                            </a>
                            <a href="{{ path('student_delete', {id:student.id}) }}" data-popup="tooltip" title="{{ 'delete' }}" data-container="body" onclick="return confirm('{{ 'Confirm your action' }}')" class="list-icons-item text-danger-600">
                                <i class="icon-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}

            {% block javascripts %}
                {{ parent() }}

                <!-- Theme JS files -->
                <script src="{{ asset('global_assets/js/plugins/tables/datatables/datatables.min.js') }}"></script>
                <script src="{{ asset('global_assets/js/plugins/tables/datatables/extensions/responsive.min.js') }}"></script>
                <script src="{{ asset('global_assets/js/plugins/forms/selects/select2.min.js') }}"></script>

                <script src="{{ asset('global_assets/js/demo_pages/datatables_responsive.js') }}"></script>
                <!-- /theme JS files -->
            {% endblock %}